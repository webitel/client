import { QueuesNamespace } from '../../../stores/namespace';

/** the legacy vuex key was `log`; the directory and everything else says logs */
export const QueueLogsNamespace = `${QueuesNamespace}/logs`;
