export const StrategyList = Object.freeze([
    { name: 'FIFO', value: 'fifo' },
    { name: 'LIFO', value: 'lifo' }
]);

export const Strategy = Object.freeze({
    FIFO: 'fifo',
    LIFO: 'lifo',
});
